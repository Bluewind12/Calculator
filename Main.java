//Create By Momose
/**
 * Main
 */
public class Main {
    //‘«‚µŽZ
    int Plus(int alpha, int beta) {
        System.out.println(alpha + " + " + beta + " = " + (alpha + beta));
        return alpha + beta;
    }

    //ˆø‚«ŽZ
    int Minus(int alpha, int beta) {
        System.out.println(alpha + " - " + beta + " = " + (alpha - beta));
        return alpha - beta;
    }

    //Š|‚¯ŽZ
    int Multiply(int alpha, int beta) {
        System.out.println(alpha + " * " + beta + " = " + (alpha * beta));
        return alpha * beta;
    }

    //Š„‚èŽZ
    int Division(int alpha, int beta) {
        System.out.println(alpha + " / " + beta + " = " + (alpha / beta));
        return alpha / beta;
    }

}